name: C/C++ CI

on:
  push:
    branches: [ "linux-rolling-stable" ]
  pull_request:
    branches: [ "linux-rolling-stable" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make ARCH=um
